DATABASE_URL="mysql://chopal:CHANGE_ME@127.0.0.1:3306/chopal?connection_limit=5"
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
NEXT_PUBLIC_SITE_NAME="Chopal Unfiltered"
GOOGLE_SITE_VERIFICATION=""
UPLOAD_DIR="./uploads"
# Never use demonstration mode on the public production site.
DEMO_MODE="false"
# Used only by the seed and admin creation commands, never a default password.
ADMIN_EMAIL=""
ADMIN_PASSWORD=""
ADMIN_NAME="Editor"
# Set true only behind a trusted proxy which overwrites X-Forwarded-For.
TRUST_PROXY="false"
